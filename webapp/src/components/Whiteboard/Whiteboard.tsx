import styled from "styled-components";

const WhiteboardUI = styled.div`
  display: flex;
  flex: 1;
  height: 100vh;
`;

function Whiteboard() {
  return <WhiteboardUI>Whiteboard</WhiteboardUI>;
}

export default Whiteboard;
